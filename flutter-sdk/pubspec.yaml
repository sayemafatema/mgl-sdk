name: mgl_fleet_sdk
description: Pure Flutter MGL Fleet driver SDK — full E2E flow via FleetNativeSdk / FleetAppEngine.
version: 0.6.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  mobile_scanner: ^6.0.2
  share_plus: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/mgl_logo.png
