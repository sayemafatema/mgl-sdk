name: mgl_fleet_native_example
description: Example host app for mgl_fleet_native_sdk (native Android/iOS fleet UI).
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  mgl_fleet_native_sdk:
    path: ../mgl_fleet_native_sdk

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
