import { Routes } from '@angular/router';
import { FleetFlowHostComponent } from './fleet-flow-host.component';

export const FLEET_SHELL_ROUTES: Routes = [
  { path: '', component: FleetFlowHostComponent },
];
